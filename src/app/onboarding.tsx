import { View, Text } from 'react-native'

/**
 * Onboarding screen component that introduces new users to the app.
 * Guides users through initial setup and feature highlights.
 */
const onboarding = () => {
  return (
    <View>
      <Text>onboarding</Text>
    </View>
  )
}

export default onboarding